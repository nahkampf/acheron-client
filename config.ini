; MODE is either 'dev' or 'prod', and determines (among other things) how much debug we get (none in production)
MODE=dev
; Determines what type of terminal this is. Valid types are:
; sensor, sigint, geoloc, science, logs, commander, public
TERMINAL_TYPE=sensor
